# HyperTicket 界面设计规范

## 设计方向

HyperTicket 是高频操作型票务产品。界面保持安静、清晰和可扫描，首屏直接展示可操作的票务目录，不使用营销式 Hero、装饰性渐变或大面积宣传卡片。

用户端以浅色内容画布、深色正文、珊瑚红强调价和低饱和青绿色品牌色为主；管理端沿用同一基础令牌，但信息密度更高。品牌图形、活动封面和票面是主要视觉资产。

## 基础令牌

| 令牌 | 建议值 | 用途 |
|---|---:|---|
| 页面背景 | `#f4f8f8` | 浏览页底色 |
| 内容背景 | `#ffffff` | 表格、表单、活动卡 |
| 主文本 | `#172526` | 标题和关键数据 |
| 次文本 | `#657576` | 元数据和说明 |
| 品牌色 | `#14756f` | 品牌、选中态和主要操作 |
| 价格色 | `#e45d3f` | 价格和应付金额 |
| 边框 | `#dce5e5` | 输入框、分隔线和卡片边界 |
| 成功色 | `#237a4b` | 已支付、已确认 |
| 警示色 | `#b56a16` | 待支付、待开售 |
| 危险色 | `#b83b3b` | 失败、取消和破坏性操作 |

圆角保持克制：输入框和按钮通常为 4-6px，内容卡不超过 8px。阴影只用于弹层、菜单和需要明确抬升的临时界面。

## 排版与层级

- 页面标题 24-32px，紧凑面板标题 16-20px，正文 14-16px。
- 不使用随视口宽度缩放的字号，不使用负字间距。
- 价格、库存和订单状态用字重和颜色强调，不依赖超大字号。
- 日期、场馆、厅馆、场次和座位采用稳定顺序，方便重复扫描。

## 布局

### 桌面端

- 顶部导航保持紧凑，搜索、类型、城市和定位操作位于同一工具行。
- 活动分区使用横向卡片轨道或规则网格，分区之间由留白和标题区分。
- 个人中心采用侧边导航与主内容区；订单、票和观演人使用明确的列表结构。
- 管理端优先使用表格、标签页、筛选栏和编辑抽屉，避免页面章节卡片化。

### 移动端

- 搜索占满首行，筛选器在下一行使用两列或横向滚动。
- 活动分区保留横向滑动提示，一次完整展示一张主卡和下一张卡片边缘。
- 关键命令位于拇指可达区域，选座与支付底栏保持固定尺寸。
- 导航、按钮、标签和价格必须在 320px 宽度内无重叠、无横向文本溢出。

## 组件规则

- 类型和城市使用自定义下拉菜单，视觉状态与输入框一致，支持键盘与触屏。
- 图标按钮使用 Lucide 图标并提供 `aria-label` 和悬停提示。
- 二元设置使用开关或复选框；有限选项使用菜单或分段控件。
- 状态反馈使用主题化 Toast、内联状态或确认弹层，不使用浏览器原生 `alert`。
- 状态文本必须同时提供图标/文案，不只依赖颜色。
- 页面区块不套用浮动卡片；卡片只用于活动、订单、票面等独立重复对象。

## 关键动效

- 支付二维码加载和扫码结果采用短促状态转换，避免伪装真实支付渠道。
- 支付成功后的出票动效模拟票纸从打印机送出，结束后必须停在可读、可操作的电子票状态。
- 菜单、弹层和 Toast 使用 150-240ms 的位移与透明度变化。
- 遵循 `prefers-reduced-motion`，关闭大幅移动和重复动画。

## 图片与地图

- 活动封面使用可公开使用的真实场景照片，并保存到 `clients/user-client/public/media/events/`。
- 来源与许可记录维护在 `clients/user-client/public/media/ATTRIBUTION.md`。
- 图片需设置稳定宽高比和 `object-fit: cover`，避免加载造成布局偏移。
- 地图与导航使用高德地图；密钥通过环境变量注入，不写入仓库。

## 可访问性与验收

- 交互控件具有可见焦点、语义标签和至少 44px 的移动端触摸区域。
- 正文和关键状态满足可读对比度，键盘可以完成菜单、表单和选座流程。
- 每次影响布局的改动至少检查桌面和 320-390px 移动视口。
- 验收覆盖空数据、加载、错误、会话过期、售罄、未开售、支付失败和取消状态。
