# HyperTicket 产品说明

## 产品定位

HyperTicket 是覆盖活动发现、选座购票、模拟支付、电子票和运营管理的票务系统实践项目。首屏直接进入票务浏览，不设置营销落地页；用户端强调快速筛选和下单，管理端强调结构化维护与状态审计。

项目当前用于本地开发、系统设计验证和演示，不代表已具备商业票务平台的生产资质。支付以 `MOCK` 为默认实现，`ALIPAY`、`WECHAT` 为开发占位 Provider。

## 用户与场景

| 角色 | 主要任务 |
|---|---|
| 游客 | 浏览、搜索和筛选公开活动，查看详情、场次与座位 |
| 注册用户 | 收藏、选座、下单、模拟支付、查看订单和电子票、维护个人资料 |
| 提醒订阅用户 | 订阅未开售场次，并在开售时接收邮件 |
| 管理员 | 维护活动目录与场次，检查开售提醒和业务状态 |

## 活动目录

正式目录采用 v10 完整模型：

```text
活动 -> 场馆/影院 -> 厅馆 -> 场次 -> 票档/座位 -> 订单
```

支持电影、演唱会、演出、脱口秀、展览、电竞赛事和体育赛事。场馆保存经纬度及高德地图导航信息；电影可按影院和影厅组织，其他活动可按场馆、厅馆、日期与场次组织。

当前演示数据包含 35 个拟真虚构活动和 70 个场次。封面存放在客户端本地资源目录，并记录来源归属，避免运行时依赖外部图片链接。

## 核心体验

### 浏览与发现

- 搜索框、活动类型和城市筛选在桌面端同一行，移动端自适应换行。
- 首页按推荐、近期必看、即将开售、正在热映与演出、热门电竞与体育、城市精选分区。
- 活动卡展示分类、城市、标题、场馆、下一场时间和起售价。
- 可预订场次数量作为页面摘要，不以旧票品库存替代真实场次数。
- 过期的可选 Session 不阻断公开目录，服务端按游客请求继续返回数据。

### 选座与下单

- 同场活动支持一次选择多个可售座位。
- 场次、票档和座位库存相互关联；已售、锁定、不可售状态不可选择。
- 下单通过 Redis Streams 异步处理，客户端按 `request_id` 查询最终结果。
- 写库失败、订单取消或超时会释放座位并补偿库存。

### 支付与出票

- 支付页以二维码模拟收银台，展示订单和应付金额。
- 模拟扫码完成后直接返回订单、支付和电子票信息。
- 已支付订单以打印机出票动效呈现，再进入稳定的订单与电子票视图。
- 所有金额在服务端以最小货币单位存储；支付请求要求客户端幂等键。

### 个人中心与账号安全

- 个人资料、头像即时生效、手机号和邮箱验证状态。
- 我的订单、我的电子票、收藏和浏览历史。
- 常用观演人维护，并可在下单时关联座位。
- 修改密码、联系方式验证、旧 Session 撤销等安全能力集中在账号安全区域。

### 开售提醒

- 用户可订阅尚未开售的场次。
- 管理员设置的时间必须满足 `售票开始 < 售票截止 < 演出开始`。
- 定时任务到达开售时间后发送邮件并写入提醒审计。
- 发送冷却、TTL、次数限制和防重放由统一 Verification Provider 负责。

## 客户端范围

用户端和管理端统一使用 React 18.3、TypeScript 5.6、Vite 6 和 Tauri 2：

- 浏览器模式通过 WebSocket-TCP 桥接访问 C++ 服务。
- Windows 桌面模式通过 Tauri Rust 命令直连 TCP。
- Qt/QML 和旧 `frontend/` 不属于当前客户端架构。

## 近期产品重点

1. 库存对账、异常队列和运营修复入口。
2. 活动级排队、风控挑战和购买限制。
3. 电子票二维码轮换、核销幂等和转赠规则。
4. 真实渠道资质具备后，再接入单一支付 Provider 的 HTTPS webhook、查单与对账。
